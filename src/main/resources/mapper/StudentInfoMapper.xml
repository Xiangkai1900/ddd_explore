<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.infrastructure.datasource.StudentInfoDao">

        <select id="selectStudentInfo" resultType="org.example.infrastructure.model.po.StudentInfoPo">
            select * from student_info
            where
            student_number =#{studentNumber}
        </select>

</mapper>
